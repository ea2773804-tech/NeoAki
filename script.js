// Script inicial da aplicação NeoAki.
// Neste momento, apenas tratamos ações simples dos botões.

const loginBtn = document.getElementById('login-btn');
const signupBtn = document.getElementById('signup-btn');

function handleLoginClick() {
  // Placeholder: no próximo passo conectaremos com formulário/autenticação.
  alert('Área de Login será implementada no próximo passo.');
}

function handleSignupClick() {
  // Placeholder: no próximo passo conectaremos com cadastro real.
  alert('Área de Cadastro será implementada no próximo passo.');
}

loginBtn.addEventListener('click', handleLoginClick);
signupBtn.addEventListener('click', handleSignupClick);
